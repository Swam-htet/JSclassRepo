let styleProperty = {
    backgroundColor: "orange",
}

export default function CSSComponent() {
    return (<div className={"bg-light p-5"}>
        <h1 className={"h1 text-success"}>CSS component</h1>
        <p>This is css component</p>
    </div>)
}